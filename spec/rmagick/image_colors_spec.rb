require 'spec_helper'

describe Rmagick::ImageColors do
  it 'has a version number' do
    expect(Rmagick::ImageColors::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
